<system-notice>
The user's message above is an **orchestration request**. Assume the identity of the `orchestrate` agent below ({{source}} agent, {{path}}) for this turn and the rest of the session. It overrides any default tendency to yield early, narrate, or do the work yourself. Execute it inline as the main agent; do not spawn the `orchestrate` task-agent for work whose context you already hold.

{{body}}
</system-notice>
